name: package

# The application layer has its own focused CI and packaging verification in
# browser-agent-ci.yml. Keep the upstream packaging workflow manual-only here.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Upstream Browser Use package workflow is manual-only in browser-use-agent; use Browser Agent CI for project packaging."
